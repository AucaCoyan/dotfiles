matches:
  - trigger: loggingbasicconfig
    replace: |
      logging.basicConfig(
      level=logging.DEBUG, format="[%(asctime)s %(levelname)s] - %(message)s"
      )

  - trigger: saveasajson
    replace: |
      with open(file='output.json')
